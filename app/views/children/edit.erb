<DOCTYPE! html>

<head>
<link href="https://fonts.googleapis.com/css2?family=Allerta+Stencil&display=swap" rel="stylesheet">
<link rel="stylesheet" href="./public/stylesheets/main.css">
</head>

<body>

<h2>Update your child's devices</h2>

  <form action="/children/<%= @child.id %>" method="POST">
    <input id="hidden" type="hidden" name="_method" value="patch">
    <label>Edit your Child's Name:</label>
      <br>
    <input type="text" name="child[name]" value="<%= @child.name %>">
      <br>

<h2>Select an existing device or create a new one below.</h2>

  <h3>Existing Devices</h3>
      <% @devices.each do |device| %>
      <input type="checkbox" name="child[device_ids][]" id="<%= device.id %>" value="<%= device.id %>" <%='checked' if @child.devices.include?(device) %>><%= device.device_type %></input><br>
      <% end %>
        <br>

  <h3>New Device</h3>
      <label>Device Name: </label>
        <br>
      <input type="text" name="device[device_type]" id="device_type"></input>
        <br>
        <br>
      <input type="submit" value="Update Device">
 
  
    </form> 
</body>
</html>